# راهنمای ساخت خروجی نرم‌افزار ویندوز (Windows .exe) - حسابداری ناهار

این پروژه با بهره‌گیری از **Electron** و **electron-builder** به سادگی به یک فایل نصبی مستقل ویندوز (`Setup.exe`) یا نسخه بدون نیاز به نصب (`Portable.exe`) تبدیل می‌شود.

---

### روش اول: اجرای خودکار با اسکریپت یک‌کلیکه (توصیه‌شده برای ویندوز)

1. سورس‌کد پروژه را از طریق منوی بالای AI Studio به صورت **ZIP** دانلود کرده و روی کامپیوتر خود استخراج (Extract) فرمایید.
2. فایل `build-windows-exe.bat` که در پروژه قرار دارد را با دو بار کلیک اجرا نمایید.
3. اسکریپت به صورت خودکار مراحل بیلد، نصب الکترون و تولید پکیج ویندوز را انجام داده و پوشه خروجی `release` حاوی فایل `.exe` را باز می‌کند!

---

### روش دوم: اجرای دستی دستورات در ترمینال یا CMD

پس از استخراج پروژه در ویندوز، ترمینال (PowerShell یا CMD) را در پوشه پروژه باز کنید و دستورات زیر را وارد کنید:

```bash
# ۱. کامپایل پروژه فرانت‌اند
npm run build

# ۲. نصب الکترون و بیلدر ویندوز
npm install --save-dev electron electron-builder

# ۳. تولید فایل نصبی استاندارد NSIS ویندوز (.exe)
npx electron-builder --win nsis --config.directories.output=release
```

پس از اتمام فرایند، فایل نصبی در پوشه `release` با نامی شبیه به:
`حسابداری ناهار Setup 1.0.0.exe`
آماده نصب و تحویل به کاربران خواهد بود.

---

### روش سوم: نصب فوری نسخه دسکتاپ ویندوز بدون بیلد (PWA Desktop)
اگر در سیستم ویندوزی خود مرورگر **Google Chrome** یا **Microsoft Edge** را باز دارید:
1. آدرس سامانه را در مرورگر باز کنید.
2. دکمه **«خروجی ویندوز (EXE)»** در نوار بالای برنامه را بزنید و دکمه **«نصب فوری در ویندوز»** را کلیک نمایید.
3. یا از نوار آدرس مرورگر، آیکون نصب (مربع با فلش یا کامپیوتر کوچک در انتهای کادر URL) را انتخاب کنید.
4. برنامه به صورت یک نرم‌افزار رسمی دسکتاپ با آیکون در دسکتاپ، منوی استارت و بدون فریم مرورگر با دیتابیس محلی کار خواهد کرد.
