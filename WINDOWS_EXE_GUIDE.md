# راهنمای ساخت و اجرای نرم‌افزار دسکتاپ ویندوز (Windows .exe) - حسابداری ناهار

این پروژه یک پوسته کامل **Electron** به همراه پیکربندی آماده **electron-builder** دارد؛ بنابراین با یک دستور به فایل نصبی استاندارد ویندوز (`Setup.exe`) و نسخه بدون نیاز به نصب (`Portable.exe`) تبدیل می‌شود.

ویژگی‌های نسخه دسکتاپ:

- اجرای کاملاً آفلاین (فونت فارسی وزیرمتن داخل خود بسته قرار دارد و به CDN نیازی نیست)
- پنجره با منوی فارسی، میان‌برهای بزرگ‌نمایی/تمام‌صفحه و به‌خاطر سپردن اندازه و موقعیت پنجره
- ذخیره فایل‌های خروجی (JSON/CSV) با پنجره بومی «Save As…» ویندوز
- جلوگیری از اجرای نمونه دوم برنامه (Single Instance) و باز شدن لینک‌های بیرونی در مرورگر پیش‌فرض
- داده‌ها در پروفایل کاربری ویندوز (localStorage/IndexedDB مختص همین اپ) نگهداری می‌شوند

---

### روش اول: اجرای خودکار با اسکریپت یک‌کلیکه (توصیه‌شده برای ویندوز)

1. سورس‌کد پروژه را دانلود/کلون کرده و روی کامپیوتر خود استخراج نمایید.
2. فایل `build-windows-exe.bat` را با دوبار کلیک اجرا نمایید.
3. اسکریپت به صورت خودکار نصب بسته‌ها، بیلد فرانت‌اند (حالت دسکتاپ) و تولید پکیج ویندوز را انجام می‌دهد و در پایان پوشه خروجی `release` را باز می‌کند.

---

### روش دوم: اجرای دستی دستورات در ترمینال (CMD / PowerShell)

پیش‌نیاز: Node.js نسخه 20 یا جدیدتر

```bash
# ۱. نصب بسته‌ها
npm install

# ۲. بیلد فرانت‌اند + تولید Setup و Portable ویندوز
npm run dist:win
```

پس از اتمام فرایند، دو فایل در پوشه `release` آماده تحویل خواهند بود:

- `NaharAccounting-Setup-1.0.0.exe` — نصب‌کننده استاندارد NSIS (با انتخاب محل نصب، میان‌بر دسکتاپ و منوی استارت)
- `NaharAccounting-Portable-1.0.0.exe` — نسخه تک‌فایلی بدون نیاز به نصب

دستورات کمکی دیگر:

```bash
npm run desktop      # اجرای سریع نسخه دسکتاپ بدون بسته‌بندی (برای تست)
npm run dist:linux   # خروجی AppImage لینوکس
npm run icons        # تولید مجدد آیکون‌ها (resources/icon.ico و icon.png) از public/icon.svg
```

---

### روش سوم: بیلد خودکار در GitHub Actions (بدون نیاز به سیستم ویندوزی)

وردفلوی `.github/workflows/build-windows.yml` با پوش کردن تگ `v*` (مثلاً `v1.0.0`) یا اجرای دستی (Workflow dispatch) روی سرویس‌دهنده ویندوز اجرا شده و فایل‌های `.exe` را به صورت Artifact آپلود می‌کند:

```bash
git tag v1.0.0
git push origin v1.0.0
```

سپس از تب **Actions** در مخزن گیت‌هاب، Artifact مربوط به `windows-installers` را دانلود کنید.

---

### روش چهارم: نصب فوری نسخه دسکتاپ بدون بیلد (PWA Desktop)

اگر در سیستم ویندوزی خود مرورگر **Google Chrome** یا **Microsoft Edge** را دارید:

1. آدرس سامانه را در مرورگر باز کنید.
2. دکمه **«خروجی ویندوز (EXE)»** در نوار بالای برنامه و سپس **«نصب فوری در ویندوز»** را بزنید؛ یا از نوار آدرس، آیکون نصب را انتخاب کنید.
3. برنامه به صورت یک نرم‌افزار دسکتاپ با آیکون در دسکتاپ و منوی استارت و بدون فریم مرورگر، با دیتابیس محلی کار خواهد کرد.

---

### یادداشت‌های فنی

- بیلد دسکتاپ با دستور `vite build --mode desktop` ساخته می‌شود؛ در این حالت `base` نسبی (`./`) است تاAppBundle از طریق پروتکل `file://` بارگذاری شود و پلاگین PWA/سرویس‌ورکر غیرفعال می‌گردد.
- آیکون برنامه از `public/icon.svg` توسط `scripts/make-icon.mjs` در ابعاد مختلف داخل `resources/icon.ico` بسته‌بندی می‌شود.
- پیکربندی بسته‌بندی در `electron-builder.yml` و پوسته دسکتاپ در `electron-main.cjs` / `electron-preload.cjs` قرار دارد.
- پوشه‌های `dist/` و `release/` خروجی بیلد هستند و در Git کامیت نمی‌شوند.
